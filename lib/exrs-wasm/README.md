# EXRS_WASM

Contains everything needed for the EXRS NPM package.

- `./` Contains the Rust glue crate itself in the top level.
- `./js` folder contains the public NPM package
- `./examples` folder contains javascript examples
- `./pgk` folder (generated locally) contains the internal wasm package